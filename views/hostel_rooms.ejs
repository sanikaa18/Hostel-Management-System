<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hostel Rooms</title>
</head>
<body>
    <h1>Hostel Rooms</h1>
    <ul>
        <% hostelRooms.forEach(function(room) { %>
            <li>
                Room Number: <%= room.room_number %>, 
                Capacity: <%= room.capacity %>, 
                Block: <%= room.hostel_block %>
            </li>
        <% }); %>
    </ul>
</body>
</html>
